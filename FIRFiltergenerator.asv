function[B,A]=FIRFiltergenerator(order,type,freq,fs)

    fc1=freq(1,1);
    fc2=freq(1,2);
    if type == 'lp'
        b = fir1(order,fc1/(fs/2))
    end

    if type == 'bp'

    end

    if type == 'hp'

    end

    if type == 'bs'

    end

end